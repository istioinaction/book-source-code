apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: catalog
spec:
  hosts:
  - catalog.istioinaction.io
  gateways:
    - mesh
  http:
  - route:
    - destination:
        host: catalog
        subset: version-v1
